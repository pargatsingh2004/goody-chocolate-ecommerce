# Copy this file to ".env" (same folder as manage.py) and fill in real values.
# .env is loaded automatically by goody/settings.py — never commit your real .env file.

# Use the console backend during development (emails print to the terminal).
# Switch to the SMTP backend to actually send email:
# EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=ps3257177@gmail.com
EMAIL_HOST_PASSWORD=Tetu@7973
EMAIL_USE_TLS=True

DEFAULT_FROM_EMAIL=ps3257177@gmail.com
SITE_URL=http://127.0.0.1:8000
